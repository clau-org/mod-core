{
  "name": "playground",
  "version": "0.0.0",
  "tasks": {
    "dev": "deno run -A --watch main.ts",
    "test": "deno test -A",
    "test:dev": "deno test -A --watch",
    "preview": "deno run -A main.ts",
    "install": "deno install -A -n playground main.ts",
    "uninstall": "deno uninstall playground"
  }
}
